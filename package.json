{
  "name": "harnesswiki-city",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Self-improving content-ops harness: trend context -> content concept -> reward score -> generation record -> harness update, traced in W&B Weave.",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "dev": "node -e \"console.log('HarnessWiki City: the generation loop is not implemented yet (see docs/STATUS.md). The dashboard is - run: pnpm dev:ui')\"",
    "typecheck:ui": "tsc --noEmit -p src/ui/tsconfig.json",
    "dev:ui": "vite",
    "build:ui": "tsc --noEmit -p src/ui/tsconfig.json && vite build",
    "preview:ui": "vite preview"
  },
  "dependencies": {
    "motion": "^12.40.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "@tailwindcss/vite": "^4.3.0",
    "@types/react": "^19.0.0",
    "@types/react-dom": "^19.0.0",
    "@vitejs/plugin-react": "^5.0.0",
    "tailwindcss": "^4.3.0",
    "typescript": "^5.6.0",
    "vite": "^7.0.0"
  }
}
