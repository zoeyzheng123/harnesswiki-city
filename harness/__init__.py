"""HarnessWiki City — Python backend package.

`harness.contracts` is the canonical schema (mirrored in TS at
`src/contracts/index.ts`). Loop / critic / generator / meta / scout / weave
modules are planned (see docs/STATUS.md); build against the stubs until then.
"""
