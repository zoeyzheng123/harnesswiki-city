node_modules/
dist/
.venv/
__pycache__/
*.pyc
.env
.env.local
*.log
.DS_Store

# Tool/agent artifacts
.playwright-mcp/
.impeccable/

# Synthetic learning data (generated on demand by scripts/synth_outcomes.py)
data/synthetic/

# Append-only prompt/outcome training memory (published to W&B Artifacts)
data/training/*.jsonl

# Loop run artifacts (regenerated each run by loop_core/loop.py) — keep the demo's
# data/generations.latest.json tracked, but not the per-run scratch.
raw/
wiki/
harness/gen_*.json
